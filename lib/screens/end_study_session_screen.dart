import 'package:flutter/material.dart';

class EndStudySessionScreen extends StatelessWidget {
  const EndStudySessionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}